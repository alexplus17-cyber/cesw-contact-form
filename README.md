# Contact Form App

A Node.js application that serves a contact form and sends emails using Nodemailer.

## Setup

1. Install dependencies: `npm install`

2. Configure email credentials in `server.js`:
   - Replace `'your-email@gmail.com'` with your Gmail address
   - Replace `'your-app-password'` with your Gmail app password

3. Run the app: `npm start`

4. Open http://localhost:3000 in your browser

## Features

- Contact form with validation
- Email sending via Nodemailer
- Spam protection with honeypot
- Responsive design

## Note

For production, use environment variables for email credentials and consider using a more secure email service.</content>
<parameter name="filePath">d:\email test\README.md